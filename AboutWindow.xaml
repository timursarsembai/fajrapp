<Window x:Class="FajrApp.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="О приложении"
        Width="400"
        Height="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        WindowStyle="None"
        AllowsTransparency="True"
        Topmost="True"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
        </Style>
    </Window.Resources>
    
    <Border Background="#1E1E1E" 
            CornerRadius="8" 
            BorderBrush="#3D3D3D" 
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="2" Opacity="0.5"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header with close button -->
            <Grid Grid.Row="0" Background="#252525">
                <TextBlock x:Name="HeaderText"
                           Text="About" 
                           FontSize="14" 
                           FontWeight="SemiBold" 
                           Margin="16,12"
                           VerticalAlignment="Center"/>
                
                <Button x:Name="CloseButton"
                        Content="✕"
                        FontSize="14"
                        Background="Transparent"
                        Foreground="#808080"
                        BorderThickness="0"
                        HorizontalAlignment="Right"
                        Padding="12,8"
                        Cursor="Hand"
                        Click="CloseButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="0,8,0,0"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#C42B1C"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
            
            <!-- Content -->
            <StackPanel Grid.Row="1" 
                        Margin="24" 
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center">
                
                <!-- App Icon -->
                <TextBlock Text="🕌" 
                           FontSize="48" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,16"/>
                
                <!-- App Name -->
                <TextBlock Text="FajrApp" 
                           FontSize="24" 
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Foreground="White"/>
                
                <TextBlock x:Name="AppDescription"
                           Text="Prayer Times Widget for Windows" 
                           FontSize="13" 
                           Opacity="0.7"
                           HorizontalAlignment="Center"
                           Margin="0,4,0,16"/>
                
                <!-- Version -->
                <TextBlock x:Name="VersionText"
                           Text="Версия 1.0.0" 
                           FontSize="13" 
                           HorizontalAlignment="Center"
                           Foreground="#0078D4"/>
                
                <!-- Update Date -->
                <TextBlock x:Name="UpdateDateText"
                           Text="Обновлено: 7 января 2026" 
                           FontSize="12" 
                           Opacity="0.6"
                           HorizontalAlignment="Center"
                           Margin="0,4,0,20"/>
                
                <!-- GitHub Link -->
                <Button x:Name="GitHubButton"
                        Content="🔗 GitHub: timursarsembai/fajrapp"
                        Background="#2D2D2D"
                        Foreground="#0078D4"
                        BorderThickness="0"
                        Padding="16,10"
                        Cursor="Hand"
                        HorizontalAlignment="Center"
                        Click="GitHubButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="4"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#3D3D3D"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
