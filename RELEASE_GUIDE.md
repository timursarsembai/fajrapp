# 📦 Руководство по созданию релиза FajrApp

## Подготовка релиза

### 1. Обновите версию

Обновите версию в `FajrApp.csproj`:
```xml
<Version>1.1.0</Version>
```

### 2. Соберите portable версию

```powershell
.\build-portable.ps1
```

### 3. Создайте ZIP архив

```powershell
Compress-Archive -Path ".\publish\portable\*" -DestinationPath ".\FajrApp-Portable-v1.1.0.zip" -Force
```

### 4. (Опционально) Создайте установщик

Если установлен [Inno Setup](https://jrsoftware.org/isinfo.php):

```powershell
.\build-installer.ps1
& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" ".\publish\install\FajrApp-Installer.iss"
```

Готовый установщик будет в `.\publish\install\Output\FajrApp-Setup.exe`

## Публикация на GitHub

### Через веб-интерфейс:

1. Перейдите на https://github.com/timursarsembai/fajrapp/releases
2. Нажмите **"Draft a new release"**
3. Заполните:
   - **Tag version**: `v1.1.0`
   - **Release title**: `FajrApp v1.1.0 - Theme Support & Improved UI`
   - **Description**: Список изменений (см. шаблон ниже)
4. Прикрепите файлы:
   - `FajrApp-Portable-v1.1.0.zip`
   - `FajrApp-Setup.exe` (если есть)
5. Нажмите **"Publish release"**

### Через GitHub CLI (gh):

```bash
gh release create v1.1.0 \
  --title "FajrApp v1.1.0 - Theme Support & Improved UI" \
  --notes-file RELEASE_NOTES.md \
  FajrApp-Portable-v1.1.0.zip
```

## Шаблон описания релиза

```markdown
## 🎨 Новые возможности

- **Темы оформления** — светлая и тёмная темы
- **Настройка прозрачности** — управление прозрачностью виджета от 30% до 100%
- **Улучшенный интерфейс настроек** — боковое меню с вкладками для лучшей организации
- **Улучшенный поиск города** — показывает до 10 результатов с указанием региона и страны

## 🔧 Улучшения

- Переработан дизайн окна настроек с боковой панелью навигации
- Автоматическая адаптация цветов для обеих тем
- Улучшенная читаемость текста в разных темах
- Централизованное управление темами через ThemeManager

## 📋 Требования

- Windows 10/11
- [.NET 8.0 Desktop Runtime](https://dotnet.microsoft.com/download/dotnet/8.0)

## 📥 Установка

### Portable версия
1. Скачайте `FajrApp-Portable-v1.1.0.zip`
2. Распакуйте в любую папку
3. Запустите `FajrApp.exe`

### Установщик (если доступен)
1. Скачайте `FajrApp-Setup.exe`
2. Запустите установщик
3. Следуйте инструкциям на экране

## 🐛 Исправленные ошибки

- Исправлена видимость текста в тёмной теме для полей координат и смещений
- Исправлен цвет фона при наведении на вкладки в светлой теме
- Исправлена проблема с цветом текста невыбранных вкладок

---

**Полный changelog**: https://github.com/timursarsembai/fajrapp/compare/v1.0.0...v1.1.0
```

## Автоматизация через GitHub Actions (будущее)

Можно настроить автоматическую сборку и публикацию релизов:

1. Создайте `.github/workflows/release.yml`
2. При создании тега `v*` будет автоматически:
   - Собираться проект
   - Создаваться архивы
   - Публиковаться релиз

Пример будет добавлен позже.

## Проверка перед релизом

- [ ] Версия обновлена в .csproj
- [ ] README.md актуален
- [ ] Протестировано на чистой системе
- [ ] Проверены обе темы (светлая/тёмная)
- [ ] Проверены все языки интерфейса
- [ ] Создан git tag
- [ ] Архивы собраны
- [ ] Релиз опубликован на GitHub
