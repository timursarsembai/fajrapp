<Window x:Class="FajrApp.PrayerTimesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Времена молитв"
        Width="320"
        Height="380"
        WindowStartupLocation="Manual"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        WindowStyle="None"
        AllowsTransparency="True"
        Topmost="True"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>
    
    <Border Background="#1E1E1E" 
            CornerRadius="8" 
            BorderBrush="#3D3D3D" 
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="2" Opacity="0.5"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Grid Grid.Row="0" Background="#252525" Margin="1,1,1,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="16,12">
                    <TextBlock Text="🕌" FontSize="18" VerticalAlignment="Center"/>
                    <TextBlock Text="Времена молитв" 
                               FontSize="16" 
                               FontWeight="SemiBold" 
                               Margin="8,0,0,0"
                               VerticalAlignment="Center"/>
                </StackPanel>
                
                <!-- Settings Button -->
                <Button Grid.Column="1" 
                        x:Name="SettingsButton"
                        Content="⚙️"
                        FontSize="16"
                        Background="Transparent"
                        Foreground="#E0E0E0"
                        BorderThickness="0"
                        Padding="8,4"
                        Cursor="Hand"
                        ToolTip="Настройки"
                        Click="SettingsButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="4"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#3D3D3D"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
                
                <!-- Close Button -->
                <Button Grid.Column="2" 
                        x:Name="CloseButton"
                        Content="✕"
                        FontSize="14"
                        Background="Transparent"
                        Foreground="#808080"
                        BorderThickness="0"
                        Padding="12,4"
                        Cursor="Hand"
                        Click="CloseButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="0,8,0,0"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#C42B1C"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
            
            <!-- Prayer Times List -->
            <StackPanel Grid.Row="1" Margin="20,16">
                <!-- Fajr -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🌙" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock x:Name="FajrLabel" Text="Фаджр" FontWeight="Medium"/>
                    </StackPanel>
                    <TextBlock x:Name="FajrTime" Grid.Column="1" Text="05:30" FontWeight="SemiBold"/>
                </Grid>
                
                <Border Height="1" Background="#3D3D3D" Margin="0,4,0,8"/>
                
                <!-- Sunrise -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🌅" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock x:Name="SunriseLabel" Text="Восход" FontWeight="Medium"/>
                    </StackPanel>
                    <TextBlock x:Name="SunriseTime" Grid.Column="1" Text="07:15" FontWeight="SemiBold"/>
                </Grid>
                
                <Border Height="1" Background="#3D3D3D" Margin="0,4,0,8"/>
                
                <!-- Dhuhr -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="☀️" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock x:Name="DhuhrLabel" Text="Зухр" FontWeight="Medium"/>
                    </StackPanel>
                    <TextBlock x:Name="DhuhrTime" Grid.Column="1" Text="12:30" FontWeight="SemiBold"/>
                </Grid>
                
                <Border Height="1" Background="#3D3D3D" Margin="0,4,0,8"/>
                
                <!-- Asr -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🌤️" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock x:Name="AsrLabel" Text="Аср" FontWeight="Medium"/>
                    </StackPanel>
                    <TextBlock x:Name="AsrTime" Grid.Column="1" Text="15:00" FontWeight="SemiBold"/>
                </Grid>
                
                <Border Height="1" Background="#3D3D3D" Margin="0,4,0,8"/>
                
                <!-- Maghrib -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🌇" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock x:Name="MaghribLabel" Text="Магриб" FontWeight="Medium"/>
                    </StackPanel>
                    <TextBlock x:Name="MaghribTime" Grid.Column="1" Text="17:45" FontWeight="SemiBold"/>
                </Grid>
                
                <Border Height="1" Background="#3D3D3D" Margin="0,4,0,8"/>
                
                <!-- Isha -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🌑" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock x:Name="IshaLabel" Text="Иша" FontWeight="Medium"/>
                    </StackPanel>
                    <TextBlock x:Name="IshaTime" Grid.Column="1" Text="19:30" FontWeight="SemiBold"/>
                </Grid>
            </StackPanel>
            
            <!-- Footer with city -->
            <Border Grid.Row="2" Background="#252525" Padding="16,10" CornerRadius="0,0,8,8" Margin="1,0,1,1">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="📍" FontSize="14" Margin="0,0,6,0"/>
                    <TextBlock x:Name="CityText" Text="Москва" Opacity="0.7" FontSize="13"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
