<Window x:Class="FajrApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FajrApp"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        SizeToContent="Width"
        Height="40"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style x:Key="WidgetTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <Style x:Key="PrayerNameStyle" TargetType="TextBlock" BasedOn="{StaticResource WidgetTextStyle}">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Opacity" Value="0.9"/>
        </Style>
        
        <Style x:Key="TimeStyle" TargetType="TextBlock" BasedOn="{StaticResource WidgetTextStyle}">
            <Setter Property="Opacity" Value="0.85"/>
        </Style>
        
        <Style x:Key="SeparatorStyle" TargetType="TextBlock" BasedOn="{StaticResource WidgetTextStyle}">
            <Setter Property="Text" Value="|"/>
            <Setter Property="Margin" Value="8,0"/>
            <Setter Property="Opacity" Value="0.5"/>
        </Style>
        
        <Style x:Key="CountdownLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource WidgetTextStyle}">
            <Setter Property="Text" Value="через"/>
            <Setter Property="Opacity" Value="0.6"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
        </Style>
        
        <Style x:Key="CountdownStyle" TargetType="TextBlock" BasedOn="{StaticResource WidgetTextStyle}">
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Opacity" Value="0.9"/>
        </Style>
    </Window.Resources>
    
    <Border x:Name="MainBorder"
            Background="#CC202020"
            CornerRadius="4"
            Padding="12,0"
            Margin="2"
            Height="32"
            VerticalAlignment="Center">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="1" Opacity="0.3"/>
        </Border.Effect>
        
        <!-- Context Menu -->
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Настройки" Click="Settings_Click"/>
                <Separator/>
                <MenuItem x:Name="AutoStartMenuItem" Header="Автозапуск" IsCheckable="True" Click="AutoStart_Click"/>
                <Separator/>
                <MenuItem Header="Обновить" Click="Refresh_Click"/>
                <MenuItem Header="О приложении" Click="About_Click"/>
                <Separator/>
                <MenuItem Header="Выход" Click="Exit_Click"/>
            </ContextMenu>
        </Border.ContextMenu>
        
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <!-- Current Prayer Info -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock x:Name="NextPrayerName" 
                           Style="{StaticResource PrayerNameStyle}" 
                           Text="Фаджр"/>
                <TextBlock x:Name="NextPrayerTime" 
                           Style="{StaticResource TimeStyle}" 
                           Margin="6,0,0,0"
                           Text="05:30"/>
            </StackPanel>
            
            <!-- Separator -->
            <TextBlock Style="{StaticResource SeparatorStyle}"/>
            
            <!-- Countdown -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Style="{StaticResource CountdownLabelStyle}"/>
                <TextBlock x:Name="Countdown" 
                           Style="{StaticResource CountdownStyle}"
                           Text="2:45:30"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
