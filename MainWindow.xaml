<Window x:Class="FajrApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FajrApp"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        SizeToContent="Width"
        Height="40"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style x:Key="WidgetTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <Style x:Key="PrayerNameStyle" TargetType="TextBlock" BasedOn="{StaticResource WidgetTextStyle}">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Opacity" Value="0.9"/>
        </Style>
        
        <Style x:Key="TimeStyle" TargetType="TextBlock" BasedOn="{StaticResource WidgetTextStyle}">
            <Setter Property="Opacity" Value="0.85"/>
        </Style>
        
        <Style x:Key="SeparatorStyle" TargetType="TextBlock" BasedOn="{StaticResource WidgetTextStyle}">
            <Setter Property="Text" Value="|"/>
            <Setter Property="Margin" Value="8,0"/>
            <Setter Property="Opacity" Value="0.5"/>
        </Style>
        
        <Style x:Key="CountdownLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource WidgetTextStyle}">
            <Setter Property="Text" Value="через"/>
            <Setter Property="Opacity" Value="0.6"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
        </Style>
        
        <Style x:Key="CountdownStyle" TargetType="TextBlock" BasedOn="{StaticResource WidgetTextStyle}">
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Opacity" Value="0.9"/>
        </Style>
        
        <!-- Pulsing animation for move mode -->
        <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="MoveModeGlow"
                           Storyboard.TargetProperty="Opacity"
                           From="0.3" To="0.8" Duration="0:0:0.6"
                           AutoReverse="True"/>
        </Storyboard>
        
        <!-- Landing animation -->
        <Storyboard x:Key="LandingAnimation">
            <DoubleAnimation Storyboard.TargetName="MainBorderScale"
                           Storyboard.TargetProperty="ScaleX"
                           From="1.05" To="1" Duration="0:0:0.15"/>
            <DoubleAnimation Storyboard.TargetName="MainBorderScale"
                           Storyboard.TargetProperty="ScaleY"
                           From="1.05" To="1" Duration="0:0:0.15"/>
        </Storyboard>
    </Window.Resources>
    
    <Border x:Name="MainBorder"
            Background="#CC202020"
            CornerRadius="4"
            Padding="12,0"
            Margin="2"
            Height="32"
            VerticalAlignment="Center"
            RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <ScaleTransform x:Name="MainBorderScale" ScaleX="1" ScaleY="1"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="1" Opacity="0.3"/>
        </Border.Effect>
        
        <!-- Context Menu -->
        <Border.ContextMenu>
            <ContextMenu x:Name="WidgetContextMenu" Placement="Top" HorizontalOffset="-50" StaysOpen="False">
                <MenuItem x:Name="MenuSettings" Header="Settings" Click="Settings_Click"/>
                <Separator/>
                <MenuItem x:Name="MenuChangePosition" Header="Change position" Click="MovePosition_Click"/>
                <MenuItem x:Name="AutoStartMenuItem" Header="Auto start" IsCheckable="True" Click="AutoStart_Click"/>
                <Separator/>
                <MenuItem x:Name="MenuAbout" Header="About" Click="About_Click"/>
                <Separator/>
                <MenuItem x:Name="MenuExit" Header="Exit" Click="Exit_Click"/>
            </ContextMenu>
        </Border.ContextMenu>
        
        <Grid>
            <!-- Move mode glow effect -->
            <Border x:Name="MoveModeGlow"
                    Background="Transparent"
                    BorderBrush="#4CAF50"
                    BorderThickness="2"
                    CornerRadius="4"
                    Margin="-12,0"
                    Opacity="0"
                    IsHitTestVisible="False">
                <Border.Effect>
                    <DropShadowEffect Color="#4CAF50" BlurRadius="15" ShadowDepth="0" Opacity="0.8"/>
                </Border.Effect>
            </Border>
            
            <!-- Move mode tooltip -->
            <Border x:Name="MoveTooltip"
                    Background="#E8202020"
                    CornerRadius="4"
                    Padding="8,4"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Center"
                    Margin="0,-45,0,0"
                    Visibility="Collapsed">
                <TextBlock x:Name="MoveTooltipText"
                          Text="Drag widget"
                          Foreground="#4CAF50"
                          FontSize="11"/>
            </Border>
            
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <!-- Current Prayer Info -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="NextPrayerName" 
                               Style="{StaticResource PrayerNameStyle}" 
                               Text="Fajr"/>
                    <TextBlock x:Name="NextPrayerTime" 
                               Style="{StaticResource TimeStyle}" 
                               Margin="6,0,0,0"
                               Text="05:30"/>
                </StackPanel>
                
                <!-- Separator -->
                <TextBlock Style="{StaticResource SeparatorStyle}"/>
                
                <!-- Countdown -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="CountdownLabel" Style="{StaticResource CountdownLabelStyle}" Text="in"/>
                    <TextBlock x:Name="Countdown" 
                               Style="{StaticResource CountdownStyle}"
                               Text="2:45:30"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
