<Window x:Class="FajrApp.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройки - FajrApp"
        Width="480"
        Height="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        Topmost="True"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3D3D3D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
        
        <!-- Dark ComboBox Item Style -->
        <Style x:Key="DarkComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3D3D3D"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#0078D4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3D3D3D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboBoxItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="2">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" 
                                                      Data="M0,0 L4,4 L8,0" 
                                                      Fill="#808080" 
                                                      HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center"
                                                      StrokeThickness="0"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Margin="10,6,26,6"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder"
                                        Background="#2D2D2D"
                                        BorderBrush="#3D3D3D"
                                        BorderThickness="1"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer>
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
        </Style>
        
        <Style TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#0078D4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,16,0,8"/>
        </Style>
        
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#3D3D3D"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock x:Name="HeaderText"
                   Grid.Row="0" 
                   Text="⚙️ Settings" 
                   FontSize="20" 
                   FontWeight="Bold"
                   Margin="0,0,0,16"/>
        
        <!-- Settings Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Language Section -->
                <TextBlock x:Name="LanguageSectionHeader" Text="🌐 Language" Style="{StaticResource SectionHeader}"/>
                
                <StackPanel Margin="0,0,0,8">
                    <TextBlock x:Name="LanguageLabel" Text="Language" Margin="0,0,0,4"/>
                    <ComboBox x:Name="LanguageComboBox" SelectionChanged="LanguageComboBox_SelectionChanged"/>
                </StackPanel>
                
                <!-- Location Section -->
                <TextBlock x:Name="LocationSectionHeader" Text="📍 Location" Style="{StaticResource SectionHeader}"/>
                
                <!-- City Search -->
                <StackPanel Margin="0,0,0,12">
                    <TextBlock x:Name="CityLabel" Text="City (enter name to search)" Margin="0,0,0,4"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="CityTextBox" 
                                 KeyDown="CityTextBox_KeyDown"/>
                        <Button x:Name="SearchButton"
                                Grid.Column="2" 
                                Content="🔍 Search"
                                Click="SearchCity_Click"
                                Padding="12,6"/>
                    </Grid>
                </StackPanel>
                
                <!-- Find My Location Button -->
                <Button x:Name="FindLocationButton"
                        Content="📍 Find my location"
                        Click="FindLocation_Click"
                        HorizontalAlignment="Left"
                        Margin="0,0,0,12"
                        Style="{StaticResource SecondaryButton}"/>
                
                <!-- Status Text -->
                <TextBlock x:Name="StatusText" 
                           Text="" 
                           Foreground="#4CAF50"
                           Margin="0,0,0,8"
                           TextWrapping="Wrap"/>
                
                <!-- Coordinates -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="LatitudeLabel" Text="Latitude" Margin="0,0,0,4"/>
                        <TextBox x:Name="LatitudeTextBox"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2">
                        <TextBlock x:Name="LongitudeLabel" Text="Longitude" Margin="0,0,0,4"/>
                        <TextBox x:Name="LongitudeTextBox"/>
                    </StackPanel>
                </Grid>
                
                <!-- Calculation Method Section -->
                <TextBlock x:Name="MethodSectionHeader" Text="🕌 Calculation Method" Style="{StaticResource SectionHeader}"/>
                
                <StackPanel Margin="0,0,0,8">
                    <TextBlock x:Name="MethodLabel" Text="Calculation Method" Margin="0,0,0,4"/>
                    <ComboBox x:Name="MethodComboBox"/>
                </StackPanel>
                
                <StackPanel Margin="0,0,0,8">
                    <TextBlock x:Name="AsrMethodLabel" Text="Asr Calculation" Margin="0,0,0,4"/>
                    <ComboBox x:Name="AsrMethodComboBox">
                        <ComboBoxItem x:Name="AsrStandardItem" Content="Standard (Shafi, Maliki, Hanbali)" Tag="0"/>
                        <ComboBoxItem x:Name="AsrHanafiItem" Content="Hanafi" Tag="1"/>
                    </ComboBox>
                </StackPanel>
                
                <!-- Time Offsets Section -->
                <TextBlock x:Name="OffsetsSectionHeader" Text="⏱️ Time Offsets (minutes)" Style="{StaticResource SectionHeader}"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Column="0" Grid.Row="0">
                        <TextBlock x:Name="FajrLabel" Text="Fajr" Margin="0,0,0,4"/>
                        <TextBox x:Name="FajrOffsetTextBox"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" Grid.Row="0">
                        <TextBlock x:Name="SunriseLabel" Text="Sunrise" Margin="0,0,0,4"/>
                        <TextBox x:Name="SunriseOffsetTextBox"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="4" Grid.Row="0">
                        <TextBlock x:Name="DhuhrLabel" Text="Dhuhr" Margin="0,0,0,4"/>
                        <TextBox x:Name="DhuhrOffsetTextBox"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="0" Grid.Row="2">
                        <TextBlock x:Name="AsrLabel" Text="Asr" Margin="0,0,0,4"/>
                        <TextBox x:Name="AsrOffsetTextBox"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" Grid.Row="2">
                        <TextBlock x:Name="MaghribLabel" Text="Maghrib" Margin="0,0,0,4"/>
                        <TextBox x:Name="MaghribOffsetTextBox"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="4" Grid.Row="2">
                        <TextBlock x:Name="IshaLabel" Text="Isha" Margin="0,0,0,4"/>
                        <TextBox x:Name="IshaOffsetTextBox"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,24,0,0">
            <Button x:Name="CancelButton" 
                    Content="Cancel" 
                    Style="{StaticResource SecondaryButton}"
                    Click="CancelButton_Click"
                    Margin="0,0,12,0"/>
            <Button x:Name="SaveButton" 
                    Content="Save" 
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
